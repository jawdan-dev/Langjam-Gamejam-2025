fn update() {
	if (1) {
		variable1 = test(1, 3, 4);
	} elif (0)
		variable1 = test(2);
	else
		variable1 = test(3);

	for i(0|variable1) {
		variable2 = variable2 * 2;
	}
	testvar = 4;

	drawtest();
}

fn test(arg) {
	return (arg * 2) + 1;
}

ball_directionX = 0.55;
ball_directionY = 0.35;
ball_speed = 10;
ball_positionX = 0.0;
ball_positionY = 0.0;
ball_sizeX = 10.0;
ball_sizeY = 10.0;

screen_sizeX = 1366.0;
screen_sizeY = 768.0;

fn drawtest() {
	velocityX = ball_directionX * ball_speed;
	velocityY = ball_directionY * ball_speed;

	if (((ball_positionX + velocityX < 0.0) + (velocityX < 0.0) = 2) + ((ball_positionX + ball_sizeX + velocityX > screen_sizeX) + (velocityX > 0.0) = 2)) {
		ball_directionX = 0.0 - ball_directionX;
		velocityX = ball_directionX * ball_speed;
	}
	if (((ball_positionY + velocityY < 0.0) + (velocityY < 0.0) = 2) + ((ball_positionY + ball_sizeY + velocityY > screen_sizeY) + (velocityY > 0.0) = 2)) {
		ball_directionY = 0.0 - ball_directionY;
		velocityY = ball_directionY * ball_speed;
	}

	ball_positionX = ball_positionX + velocityX;
	ball_positionY = ball_positionY + velocityY;

	draw rect(ball_positionX|ball_positionY|ball_sizeX|ball_sizeY);
}

fn activitytest() {
	if (0) {
		activityvar1 = 86;
	} elif (0) {
		activityvar2 = 64;
	} else {
		activityvar1 = test(10);
		activityvar2 = activityvar2 + 100;
	}
	return 5;
}